# Changelog

## 1.0.0 — 2026-09-20

- 完成首个正式版界面：统一 Windows 10 风格 Acrylic 外观、深浅色可读性、紧凑导航及首页、历史、设置三页布局。
- 首页支持单视频全部分P提取与混合多链接批量任务；结果阅读区新增可拖动时间轴、时间提示、复制、导出和快速开始下一项。
- 历史记录覆盖主页与局域网 API 任务，支持持久化、搜索、分页、重新导出、再次提取及删除。
- 设置统一收纳提取来源、B站账号、ASR、提取 API、开机自启和外观选项，并对运行中任务保持配置快照隔离。
- 默认自动启动专用登录浏览器、检查登录状态并启动带 Bearer Key 的局域网提取 API。
- 优化 Acrylic 合成性能与实色回退，修复窗口移动、标题栏交互、进度状态、结果滚动及多处布局问题。
- 安装版支持当前用户安装、覆盖升级、开始菜单入口和完整卸载；同时提供独立便携 ZIP。

## 0.7.0 — 2026-09-14

- 打开应用后自动启动专用登录浏览器，并自动检查、持续短时监听 B站登录状态；已有登录资料时无需再点击按钮。
- 重做为简洁标题栏 Acrylic 三页式界面：首页只保留链接输入，历史与全部设置分别集中到独立页面。
- 单视频自动提取全部分P；多链接自动并行提取并分别导出到 `文档\\Bili文稿`，任务结果和 API 任务统一写入 SQLite 历史。
- 提取 API 默认随软件启动，可在设置中随时停止；开机自启也移动到“常规”设置。
- 新增系统/浅色/深色主题、透明效果回退、历史搜索分页和按需加载正文。
- 设置中的来源诊断会真实探测全部分P和四条来源路线；ASR 连接测试改为后台执行，不再阻塞界面。
- 修正 Acrylic 被不透明根背景遮住、批量取消丢失已完成结果、开机自启状态刷新被覆盖等交互问题。
- 将高开销的 Accent Acrylic 改为独立 Win32 + Windows Composition 背景层；移动和缩放窗口时不再让 Qt 透明窗体承担持续模糊刷新，能力不可用时仍回退实色主题。

## 0.6.0 — 2026-07-27

- 新增随桌面软件运行的局域网 REST API，其他桌面程序、后端和脚本可以异步提交单视频文稿提取任务。
- 提供健康检查、OpenAPI、能力查询、任务状态、取消及 JSON、Markdown、TXT、SRT 结果接口。
- 提取接口默认监听 `0.0.0.0:8766`、默认关闭，并使用自动生成的 Bearer API Key 鉴权。
- API 任务复用界面的固定提取规则和当前设置快照；每个任务处理全部分P，不复制另一套提取逻辑。
- API 最多并行 3 个任务、保留 32 个非终态任务，完成结果在内存中保留 1 小时。
- 新增 API 启停、随软件启动、端口、密钥、局域网地址和活跃任务设置界面。
- 补全 ASR 模型持久化；停止服务或退出软件时会取消并等待 API 任务结束。

## 0.5.2 — 2026-07-19

- 将 API 音频转录总超时从 15 分钟延长到 1 小时（3600 秒），适合长视频和较慢的 ASR 服务。

## 0.5.1 — 2026-07-19

- 改进 ASR API 连接失败提示：针对端口未监听的 `WinError 10061` 显示 CrisperWeaver 启动条件和 `/health` 检查命令。

## 0.5.0 — 2026-07-19

- 新增 OpenAI 兼容 ASR API 后端，可连接 CrisperWeaver / MiMo 等本地服务。
- 新增 API 地址、密钥设置和 `/health` 连接测试；默认 `http://127.0.0.1:8765/v1`、`mimo-asr`、`zh`。
- 支持 `verbose_json` 分段响应和纯文本响应；API 请求取消、超时和错误会显示明确提示。
- 自动检测在本地 ASR 不可用时会尝试 API；手动选择 API 时不会启动本地 Python ASR。

## 0.4.0 — 2026-07-19

- 新增批量处理：从混合文本识别 B站链接、BV 号和 av 号，按出现顺序去重。
- 支持 1–4 路并行提取每个视频的全部分P，并分别导出独立 Markdown 文件。
- 单个视频失败不会中断其他任务；同名文件自动追加序号，支持取消批量任务。

## 0.3.0 — 2026-07-16

- 新增标准 Windows 安装包，支持开始菜单、可选桌面快捷方式和完整卸载。
- 新增“检测方式”，逐分P显示公开、匿名、登录浏览器和 ASR 是否可用。
- 新增五种精确模式：智能、只用公开、只用匿名、只用登录浏览器、只用本地 ASR。
- 所有来源最多尝试两次，第一次失败后固定等待 1 秒再重试。
- 移除底层 HTTP 隐式重试，确保单个来源不会在两次来源尝试之外额外请求。
- 重写智能排序：人工中文字幕 → B站中文 AI 字幕 → 其他 B站字幕 → 本地 ASR。
- 公开英文字幕不再阻止匿名或登录浏览器中的中文 AI 字幕。
- 字幕 URL 下载失败或正文为空时会重试并继续下一候选，不再提前终止整个分P。
- 新增真实登录状态显示及“检查登录”操作。

## 0.2.0 — 2026-07-16

- 移除 Codex 浏览器桥接和 `localhost:3456` 依赖。
- 新增独立 Edge / Chrome / Brave 专用登录浏览器。
- 新增标准库实现的本机 Chrome DevTools WebSocket 客户端。
- 固定字幕回退规则：公开视频接口 → 匿名播放器接口 → 专用登录浏览器 → 本地 ASR。
- 登录信息使用独立浏览器配置目录，不读取用户默认浏览器 Cookie。

## 0.1.0 — 2026-07-16

- 初始桌面界面、分P提取、本地 ASR 与四种导出格式。
